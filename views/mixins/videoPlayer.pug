mixin videoPlayer(video = {})
  .videoPlayer 
    video(src=`/${video.fileUrl}`) 
    .videoPlayer__controls 
      .videoPlayer__column 
        span 
          i.fas.fa-play
        span 
          i.fas.fa-volume-up
        span 00:00 / 10:00
      .videoPlayer__column 
        .time 
          .time__current
          .time__scrubber-container 
            .time__scrubber
      .videoPlayer__column 
        span 
          i.fas.fa-expand